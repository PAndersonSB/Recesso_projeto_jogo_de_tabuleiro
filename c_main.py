from v_tela_start import *

#try conecção com banco de dados (chama uma funcao de outro arquivo)
	#else try arquivo local (chama uma funcao de outro arquivo)
	#else cria arquivo local (chama uma funcao de outro arquivo)
	
	#criação de telas
	#criação do tabuleiro do jogo (criação do jogo)
	# chama a tela(V) , adiciona o numero(M) verifica de estar certo ...
criar_tela_start_loop()